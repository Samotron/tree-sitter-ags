; Text objects for AGS files
; Allow selecting logical units like groups, rows, and fields

(group) @class.outer
(group_row) @class.inner

(data_row) @function.outer
(heading_row) @function.outer
(unit_row) @function.outer
(type_row) @function.outer

(quoted_field) @parameter.inner
(field_list) @parameter.outer

(field_content) @string.inner